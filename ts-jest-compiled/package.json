{
  "name": "ts-jest-compiled",
  "private": true,
  "scripts": {
    "test": "jest --config jest-config.json",
    "build": "rm -rf build && tsc --project tsconfig.json",
    "build2": "rm -rf build && ttsc --project tsconfig.json",
    "test:compiled": "cd build && jest --config ../jest-config-compiled.json"
  },
  "devDependencies": {
    "@types/jest": "26.0.9",
    "jest": "26.3.0",
    "ts-jest": "26.2.0",
    "ts-node": "8.10.2",
    "ttypescript": "1.5.10",
    "typescript": "3.9.7"
  }
}
